#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""
إصلاح نصوص الشرح للدرس الأول في g10s1.ts (الكميات القياسية والمتجهة)
النهج: تقسيم النص إلى عناصر متعددة - نص عربي خالص + معادلات منفصلة في سطور
"""

import re
from pathlib import Path

FILE = Path("/home/z/my-project/src/lib/physics/g10s1.ts")

src = FILE.read_text(encoding="utf-8")

# البحث عن كتلة explanation الخاصة بالدرس الأول
# نحدد موقعها من "الكميات الفيزيائية نوعان" إلى نهاية كتلة explanation (قبل  examples)
# نهج: نستبدل الأسطر من 64 إلى 68

# ابحث عن بداية الكتلة
start_marker = '    "الكميات الفيزيائية نوعان:'
start_idx = src.find(start_marker)
assert start_idx >= 0, "لم يُعثر على بداية الكتلة"

# ابحث عن نهاية الكتلة: السطر الذي يبدأ بـ "الضرب المتجهي"
end_marker_idx = src.find('    "الضرب المتجهي', start_idx)
assert end_marker_idx >= 0, "لم يُعثر على نهاية الكتلة"

# ابحث عن نهاية السطر الأخير (السطر الذي ينتهي بـ .",)
end_of_line = src.find('\n', end_marker_idx)
assert end_of_line >= 0

# استخراج الكتلة القديمة
old_block = src[start_idx:end_of_line]

print("=== الكتلة القديمة ===")
print(repr(old_block[:200]))
print("...")
print(repr(old_block[-100:]))
print(f"الطول: {len(old_block)}")

# الكتلة الجديدة - نستخدم نفس المسافات البادئة
new_block = '''    "الكميات الفيزيائية نوعان: قياسية ومتجهة. القياسية لها مقدار فقط مثل الكتلة (خمسة كيلوغرام) والزمن (عشر ثوانٍ) ودرجة الحرارة (خمس وعشرون درجة مئوية). المتجهة لها مقدار واتجاه مثل القوة (عشر نيوتن للأعلى) والإزاحة (خمسة أمتار شرقًا) والسرعة المتجهة (عشرون مترًا في الثانية شمالًا).",
    "يُمثّل المتجه بسهم: طول السهم يمثل المقدار، واتجاه السهم يمثل اتجاه المتجه. يُكتب المتجه برمز فوقه سهم أو بحرف عريض.",
    "يمكن تحليل أي متجه في المستوي إلى مركبتين عموديتين: أفقية ورأسية، حيث ثيتا الزاوية بين المتجه والمحور الأفقي الموجب.",
    "A_x = |A| \\\\cos(\\\\theta)",
    "A_y = |A| \\\\sin(\\\\theta)",
    "وبالعكس يمكن إيجاد مقدار المتجه من مركباته بنظرية فيثاغورس:",
    "|A| = \\\\sqrt{A_x^2 + A_y^2}",
    "الضرب القياسي بين متجهين ينتج عددًا قياسيًا، ويُستخدم لحساب الشغل.",
    "\\\\vec{A} \\\\cdot \\\\vec{B} = |A||B|\\\\cos(\\\\theta)",
    "W = \\\\vec{F} \\\\cdot \\\\vec{d} = |F||d|\\\\cos(\\\\theta)",
    "الضرب المتجهي ينتج متجهًا جديدًا عموديًا على المستوي الذي يحوي المتجهين، ويُستخدم في حساب العزم وقوة لورنتز.",
    "\\\\vec{A} \\\\times \\\\vec{B} = |A||B|\\\\sin(\\\\theta)\\\\,\\\\hat{n}",'''

# استبدال
new_src = src[:start_idx] + new_block + src[end_of_line:]

FILE.write_text(new_src, encoding="utf-8")
print("\n✓ تم تحديث g10s1.ts بنجاح")
print(f"  حجم الملف قبل: {len(src)} حرف")
print(f"  حجم الملف بعد: {len(new_src)} حرف")
